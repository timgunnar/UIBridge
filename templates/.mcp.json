{
  "mcpServers": {
    "uibridge": {
      "command": "python",
      "args": ["-m", "uibridge.mcp_server"],
      "description": "UI 自动化测试脚本智能生成 — 录制浏览器操作 → 生成测试代码"
    },
    "playwright": {
      "command": "npx",
      "args": ["@anthropic/mcp-server-playwright", "--headless"],
      "description": "Playwright 浏览器自动化 — 用于页面分析和录制"
    }
  }
}
